import Link from 'next/link'
import { ArrowUpRight, MapPin, Trophy, Volleyball } from 'lucide-react'
import { Badge } from '@/components/ui/badge'
import { Card, CardContent, CardFooter } from '@/components/ui/card'
import { formatVND } from '@/lib/format'
import type { Court } from '@/lib/types'
const gradients = ['from-emerald-700 to-teal-400', 'from-slate-800 to-emerald-500', 'from-cyan-700 to-sky-400', 'from-lime-700 to-emerald-400']
export function CourtCard({ court, index = 0 }: { court: Court; index?: number }) { return <Card className="group overflow-hidden border-0 shadow-sm transition hover:-translate-y-1 hover:shadow-lg"><div className={`relative flex h-32 items-end bg-gradient-to-br ${gradients[index % gradients.length]} p-5`}><div className="absolute right-5 top-5 rounded-full bg-white/15 p-3 text-white backdrop-blur"><Volleyball /></div><div className="text-white"><p className="text-xs font-medium uppercase tracking-widest text-white/75">Sân thể thao</p><h3 className="text-xl font-semibold">{court.name}</h3></div></div><CardContent className="flex flex-col gap-3 p-5"><div className="flex items-center justify-between"><Badge variant="secondary">{court.type}</Badge><span className="text-sm font-semibold text-primary">{formatVND(Number(court.pricePerHour))}<span className="font-normal text-muted-foreground">/giờ</span></span></div><div className="flex items-start gap-2 text-sm text-muted-foreground"><MapPin className="mt-0.5 size-4 shrink-0 text-primary" /> <span>{court.address || 'Chưa cập nhật địa chỉ'}</span></div><p className="text-sm text-muted-foreground">Mở cửa {court.openTime} – {court.closeTime}</p></CardContent><CardFooter className="p-5 pt-0"><Link href={`/courts/${court.id}`} className="flex w-full items-center justify-between rounded-lg bg-muted px-4 py-2.5 text-sm font-semibold transition group-hover:bg-primary group-hover:text-primary-foreground">Xem chi tiết <ArrowUpRight /></Link></CardFooter></Card> }
